name: Coco's Concierge deployment
on:
  push:
    branches:
      - main
jobs:
  "Concierge-Deployment":
    runs-on: ubuntu-latest
    steps:
      - name: Cloning and setting up repository on Droplet
        uses: appleboy/ssh-action@v0.1.8
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.KEY }}
          script: |
            # Stop and remove repo if it exists
            cd development
            if [ -d cocos-concierge ];then echo cd cocos-concierge; docker compose down; cd ../; rm -rf cocos-concierge; fi
            git clone git@github.com:neutonfoo/cocos-concierge.git
            cd cocos-concierge
            ./reverse-proxy/nginx-conf-generator.sh
            docker compose up -d --build

# name: GitHub Actions Demo
# run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
# on: [push]
# jobs:
#   Explore-GitHub-Actions:
#     runs-on: ubuntu-latest
#     steps:
#       - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
#       - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
#       - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
#       - name: Check out repository code
#         uses: actions/checkout@v3
#       - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
#       - run: echo "🖥️ The workflow is now ready to test your code on the runner."
#       - name: List files in the repository
#         run: |
#           ls ${{ github.workspace }}
#       - run: echo "🍏 This job's status is ${{ job.status }}."
